# Ignore original large GeoTIFF file (kept locally, split version pushed to GitHub)
data/COGs_Tiff/Mercator_Versions/Area1/20250919_Hutchinson_Area1_L2_Photo_RGB_NoHoleFilling_Fill_RC_Ortho_Cog.tif

# Ignore any other potentially large files
*.zip

data/Working_Tiff_Layers/

node_modules/
dist/
.vite/

# Local PotreeConverter output is required for local 3D demos, but the octree
# binaries exceed GitHub's 2 GB per-object LFS limit. Keep these files locally
# under data/Potree/ and distribute them outside GitHub if needed.
data/Potree/
